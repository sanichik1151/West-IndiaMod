faction_goal_total_hegemony = {
	name = total_hegemony_goal_name
	description = total_hegemony_goal_desc
	category = long_term
	group = FOCUS_FILTER_ANNEXATION
	completed = {
		collection_size = {
			input = {
				input = game:all_countries
				operators = {
					limit = {
						OR = {
							is_in_faction_with = ROOT
							is_subject_of = ROOT
						}
					}
				}
				name = HEGEMONY_LIST
			}
			value > 15
		}
	}
	complete_effect = {
		add_faction_power_projection = 300
		every_faction_member = {
			add_faction_initiative = 2
			add_stability = 0.1
		}
	}
	ai_will_do = {
		base = 1
	}
}

faction_goal_global_arsenal = {
	name = global_arsenal_goal_name
	description = global_arsenal_goal_desc
	category = long_term
	group = FOCUS_FILTER_INDUSTRY
	completed = {
		count_in_collection = {
			collection = {
				input = game:scope
				operators = { faction_members }
				name = GLOBAL_INDUSTRY
			}
			num_of_military_factories > 300
		}
	}
	complete_effect = {
		add_faction_power_projection = 250
		every_faction_member = {
			add_tech_bonus = {
				name = global_arsenal_bonus
				bonus = 1.0
				uses = 2
				category = electronics
			}
		}
	}
	ai_will_do = {
		base = 1
	}
}